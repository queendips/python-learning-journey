# strip()
# Removes spaces from both the beginning and the end of a string.
name = "  Queen   "
print(name.strip())

# lstrip()
# Removes spaces from the left side (beginning) of a string.
print(name.lstrip())

# rstrip()
# Removes spaces from the right side (end) of a string.
print(name.rstrip())